1.降低执行频率
试图降低执行频率，看是否可不调用，若不能，看能不能少调用

2.分段执行
对效率有影响的接口，但不又不能减少调用次数，看是否能分段执行，一次执行一部分，确保不影响阻塞主逻辑，如redis的rehash的做法

3.利用缓存
在快与慢之间增加一个缓存层，如cpu与内存之间在L1、L2级cpu缓存，内存与硬盘之间有page页面缓存

4.调整代码结构
查看代码结构是否合理，或者是否有更有效算法
